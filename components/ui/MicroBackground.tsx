"use client";

/**
 * Optional background layer. Currently a no-op so the body’s solid
 * background (var(--background)) is used with no gradients or orbs.
 */
export function MicroBackground() {
  return null;
}
